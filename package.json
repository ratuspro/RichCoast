{
  "name": "richcoast",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "2D portrait-mode mobile-browser arcade game (merge + ball-split). Two zones, two devs, one contract.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}